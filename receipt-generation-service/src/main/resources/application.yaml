spring:
  profiles:
    active: dev
---
spring:
  config:
    activate:
      on-profile: key-vault
  cloud:
    azure:
      keyvault:
        secret:
          property-sources:
            - name: key-vault-property-source
              endpoint: ${AZURE_KEY_VAULT_ENDPOINT}